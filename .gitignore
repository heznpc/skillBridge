node_modules/
.DS_Store
dist/
.env
.env.local
.idea/

# Test coverage
coverage/

# Playwright E2E artifacts
test-results/
playwright-report/
playwright/.cache/

# Build artifacts
*.zip
*.crx
*.pem

# Claude Code local state (skills under .claude/skills/ ARE tracked)
.claude/settings.local.json
.claude/worktrees/

# Packaged skill bundles (build artifacts of .claude/skills/*)
*.skill

# Media files (not part of the extension)
*.mp4
*.gif
*.webm
*.png
!assets/**/*.png
# Store screenshots + promo tile ARE tracked deliverables (like the icons above)
!store-assets/*.png

# Product screenshots used by the GitHub Pages landing
!site/public/images/*.png
!docs/images/*.png

# Astro landing build state
site/dist/
site/.astro/

# Local landing QA artifacts
.preview/

# Generated store-listing copy (source of truth: store-assets/STORE_LISTING.md)
store-assets/description.md

# Stray files in root
/content.css
/skillBridge logo.png

# playwright-mcp server session dumps (accidentally committed once via #217)
.playwright-mcp/

# Dashboard exports — nothing in the build reads them.
csv/

TODO.md
snapshots/*/sources/
docs/TELEMETRY_DESIGN.md
*session*.json
scratch/
