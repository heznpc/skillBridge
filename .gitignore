node_modules/
.DS_Store
dist/
.env
.env.local
.idea/

# Test coverage
coverage/

# Playwright E2E artifacts
test-results/
playwright-report/
playwright/.cache/

# Build artifacts
*.zip
*.crx
*.pem

# Claude Code local state (skills under .claude/skills/ ARE tracked)
.claude/settings.local.json
.claude/worktrees/

# Packaged skill bundles (build artifacts of .claude/skills/*)
*.skill

# Media files (not part of the extension)
*.mp4
*.gif
*.webm
*.png
!assets/**/*.png
# Store screenshots + promo tile ARE tracked deliverables (like the icons above)
!store-assets/*.png

# Generated store-listing copy (source of truth: store-assets/STORE_LISTING.md)
store-assets/description.md

# Stray files in root
/content.css
/skillBridge logo.png

# playwright-mcp server session dumps (accidentally committed once via #217)
.playwright-mcp/

# CWS dashboard metric exports (installs/removals/WAU by region, language, OS).
# Untracked on purpose: no build script or gate reads them, they are stale the
# day after export, and they are listing business metrics rather than anything
# the extension is built from. Re-export from the dashboard when needed.
csv/

# Internal planning — this repository is public.
# TODO.md is a working board, not documentation: it carries legal posture
# (trademark contact and rebrand contingencies), outreach timing, competitive
# framing, and owner decisions that are nobody else's business. Untracked so
# it can stay useful without being published.
TODO.md
