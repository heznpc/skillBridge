/**
 * Puter.js SDK Loader
 * Handles loading and initialization of the Puter.js SDK
 * Used for free AI inference (GLM-4-Flash) without API keys
 */

(function() {
  'use strict';

  // Skip if already loaded
  if (window.__puterSDKLoaded) return;
  window.__puterSDKLoaded = true;

  console.log('[Skilljar i18n] Puter SDK loader initialized');
})();
